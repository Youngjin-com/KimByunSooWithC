#include <stdio.h>

int main(){
    printf("100과 0 사이의 숫자에서 특정 숫자의 위치를 파악해 봅시다. \n");
    int num = 30;
    
    if(num >= 50){
        printf("%d는 절반 이상에 위치합니다.\n", num);
    } else if{
        printf("%d는 절반 미만에 위치합니다.\n",num);
    }

    return 0;
}